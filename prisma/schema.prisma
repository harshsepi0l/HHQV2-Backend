generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DB_URL")
}

model TestTable {
  idTestTable Int @id
}

model TestUser {
  id   Int    @id @default(autoincrement())
  name String
  age  Int
  // ... other fields ...
}
